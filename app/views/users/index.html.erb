<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">

<!--%= render "users/info", user: @user %>-->
<%= render 'books/form', book: Book.new %>
　　
　　</div>
　　
<div class="col-md-8 offset-md-1">
      <h1>Users</h1>
      <table class="table">
        <thead>
          <tr>
            <th>Image</th>
            <th>name</th>
            <th></th>
          </tr>
        </thead>
        
        <tbody>
          <% @users.each do |user| %>
            <tr>
              <td><%= image_tag url_for(user.profile_image), size:'80x80' %></td>
              <td><%= user.name %></td>
              <td><%= link_to "show", user_session_path %></td>
              
            </tr>
          <% end %>
        </tbody>
      </table>
      </div>
      
  </div>
</div>
　　