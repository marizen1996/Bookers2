  <div>
      <%= flash[:notice] %>
  </div>

  <h2>User info</h2>

  <% if user.profile_image.attached? %>
  <%= image_tag url_for(user.profile_image), class: "img-thumbnail", size: '100x100' %>
<% else %>
  <p>画像がアップロードされていません</p>
<% end %>

<table class="table">
  <tbody>
 <tr>
   <th>Name</th>
   <th><%= user.name %></th>
 </tr>

 <tr>
   <th>Intoroduction</th>
   <th><%= @user_introduction %></th>
 </tr>
 </tbody>
 </table>

  <div class="row">
  <a class="btn btn-outline-secondary btn-block",<%= link_to edit_user_path(current_user) do %>
 <i class="fa-solid fa-user-gear"></i>
  <% end %>
  </a>

  </div>